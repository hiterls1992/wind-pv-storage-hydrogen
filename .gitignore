# ============================================================================
# 多能互补风光储氢分析软件 —— Git 忽略规则
# ============================================================================
# ⚠ 重要：本文件的首要目的是阻止「真实工程数据」进入公开仓库。
#    input.xlsx 为实际项目的 8760 小时风光序列，电价/氢价/投资单价等财务参数
#    属于商务敏感信息。一旦推送为公开仓库，内容会被第三方快照抓取，无法真正撤回。
#    如需在仓库内提供示例数据，请另行生成脱敏样例（文件名见文末白名单说明）。
# ============================================================================

# ---------- 真实工程数据（禁止公开） ----------
input.xlsx
OUTPUT-*.xlsx
数据汇总*.xlsx
概算结果*.xlsx
经评批量结果*.xlsx
*.xls
*.xlsm

# ---------- 测试与运行产物（可重新生成，不入库） ----------
tests/*-report.txt
tests/_*
图表_*/
simulation_results/

# ---------- 工作区私有数据 ----------
.workbuddy/

# ---------- 依赖与构建 ----------
node_modules/
vendor-dist/
dist/
build/

# ---------- 编辑器与 IDE ----------
.vscode/
.idea/
*.code-workspace
.codebuddy/
*.suo
*.user

# ---------- 操作系统 ----------
.DS_Store
Thumbs.db
desktop.ini
ehthumbs.db
$RECYCLE.BIN/

# ---------- 日志与临时文件 ----------
*.log
logs/
*.tmp
*.bak
*~
~$*
*.crdownload

# ============================================================================
# 白名单说明
# 若之后要提供脱敏示例数据，建议命名 input-sample.xlsx（不含真实项目序列），
# 并在 README 中说明其来源与用途；届时把该文件名加入下方的例外规则即可：
#   !input-sample.xlsx
# ============================================================================
