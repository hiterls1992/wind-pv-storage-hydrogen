# ============================================================================
# 多能互补风光储氢分析软件 —— Git 忽略规则
# ============================================================================
# 关于 input.xlsx（2026-09-12 决定）：
#   该文件为真实项目的 8760 小时风光单 MW 电量序列，仅含两列
#   「光伏电量(MWh/MW)」「风电电量(MWh/MW)」，不含电价、氢价、投资单价等
#   商务参数，按用户要求纳入本仓库，使 clone 后可直接运行仿真与优化。
#   仓库当前为 **私有**。⚠ 该文件已进入 git 历史，若将来把仓库转为公开，
#   必须先清理历史，仅删除工作区文件无法撤回。
#   仍然忽略的是：概算/经评等含商务参数的输出表（见下）。
# ============================================================================

# ---------- 数据文件 ----------
# input.xlsx 有意纳入版本管理，故不在此忽略
OUTPUT-*.xlsx
数据汇总*.xlsx
概算结果*.xlsx
经评批量结果*.xlsx
*.xls
*.xlsm

# ---------- 测试与运行产物（可重新生成，不入库） ----------
tests/*-report.txt
tests/_*
# 性能基准：baseline 是参照基线（入库），last 为每次运行产物（不入库）
tests/benchmark-last.json
图表_*/
simulation_results/

# ---------- 工作区私有数据 ----------
.workbuddy/

# ---------- 依赖与构建 ----------
node_modules/
vendor-dist/
dist/
build/

# ---------- 编辑器与 IDE ----------
.vscode/
.idea/
*.code-workspace
.codebuddy/
*.suo
*.user

# ---------- 操作系统 ----------
.DS_Store
Thumbs.db
desktop.ini
ehthumbs.db
$RECYCLE.BIN/

# ---------- 日志与临时文件 ----------
*.log
logs/
*.tmp
*.bak
*~
~$*
*.crdownload

# ============================================================================
# 白名单说明
# 若之后要额外提供脱敏示例数据，建议命名 input-sample.xlsx，
# 并在 README 中说明其来源与用途；届时把该文件名加入下方的例外规则即可：
#   !input-sample.xlsx
# ============================================================================
